  type Record {
    id: ID!
    date: String!
    time: String!
    occupied: Int!
    free: Int!
  }
  type FullInfo{
      records: [Record]
      Weather: String

  }

  type Query {
      getInfo: FullInfo
  }
